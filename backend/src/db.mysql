const knex = require('knex')({
        client: 'mysql',
        connection: {
            host : '127.0.0.1',
            port : 3001,
            user : 'user',
            password : 'senha',
            database : 'database'
        },
    pool: { 
        min: 0, 
        max: 7 }
});